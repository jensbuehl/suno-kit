/** Build-time debug flag. Set to true to enable verbose debug/warn logging. */
export const DEBUG = false;

/** CDN base for Suno media files (audio, cover, video). */
export const CDN_BASE = 'https://cdn1.suno.ai';

/** Suno studio API base used for aligned-lyrics requests. */
export const STUDIO_API_BASE = 'https://studio-api.prod.suno.com/api/gen';
